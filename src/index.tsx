import React from "react";
import ReactDOM from "react-dom";
import ReactKeypad from "./ReactKeypad";

ReactDOM.render(<ReactKeypad isVisible onClose={() => null} />, document.getElementById("root"));
